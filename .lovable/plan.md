

## План: Обновить стили кнопок CTA

### Что меняем

1. **«Согласовать тему с редактором»** — до нажатия: зелёный ободок (контурная кнопка `border-accent text-accent`). После нажатия — заливается зелёным (`bg-accent text-accent-foreground`), раскрывается выбор мессенджера.

2. **«Хочу подумать»** — серая кнопка (уже так). После нажатия «Согласовать тему» — кнопка «Хочу подумать» исчезает.

### Файл: `src/components/quiz/QuizResults.tsx`

**Строки 353–366 (главная кнопка):**
- Стиль до клика: `border-2 border-accent text-accent bg-transparent` (зелёный ободок)
- Стиль после клика (showMessengerChoice=true): кнопка заливается `bg-accent text-accent-foreground`

**Строки 387–401 (кнопка «Хочу подумать»):**
- Обернуть в `{!showMessengerChoice && (...)}` — скрывать при выборе мессенджера
- Стиль остаётся серым (`bg-muted text-muted-foreground`)

